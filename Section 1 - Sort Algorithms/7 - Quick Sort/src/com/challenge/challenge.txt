# Challenge:
- Modify the quick sort algorithm so that it sorts integers in DESCENDING order.
- Use the usual example array.

TIP: Write your solution int a Solution class which is created inside challenge package.